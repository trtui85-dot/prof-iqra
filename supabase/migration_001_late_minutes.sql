-- ============================================================
-- أساتذة اقرأ — تعديلات قاعدة البيانات (إصدار 1)
-- نفّذ هذا الكود في: Supabase Dashboard > SQL Editor > Run
-- آمن للتكرار (if not exists) — يمكنك تشغيله حتى لو نُفّذ سابقاً.
-- ============================================================

-- إضافة عمود عدد دقائق التأخير في سجل الحضور
-- (يُسجَّل عند المسح بالـ QR أو عند التسجيل اليدوي من لوحة الإدارة)
alter table attendance_logs
  add column if not exists late_minutes int;